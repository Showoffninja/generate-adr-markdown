{
  "name": "generate-adr-markdown",
  "version": "1.0.0",
  "description": "This GitHub Action converts issues with a specific label into Architectural Decision Records (ADR) markdown files upon issue closure. The ADR files are organized into sub-folders based on their status (`proposed`, `accepted`, `rejected`).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
     "build": "tsc && ncc build lib/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@types/node": "^22.15.0",
    "@vercel/ncc": "^0.38.3"
  }
}
